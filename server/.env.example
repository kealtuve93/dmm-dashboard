# Dance Motion Marketing Dashboard - Environment Configuration

# Server Port
PORT=3001

# BigQuery Configuration
BIGQUERY_PROJECT_ID=dance-reporting
BIGQUERY_CREDENTIALS_PATH=../config/bigquery-credentials.json

# Demo Mode
# Options: 'auto' (auto-detect), 'demo' (force demo mode), 'bigquery' (force BigQuery mode)
DEMO_MODE=auto
